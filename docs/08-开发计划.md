# ByteCoach 开发计划（基于代码审查）

> 制定日期：2026-05-02
> 前置文档：[07-代码审查与评分报告](./07-代码审查与评分报告.md)
> 目标：将综合评分从 6.6 提升至 8.0+，补齐核心闭环，建立质量基线

---

## 一、计划总览

当前项目综合评分 6.6，最大短板是功能完整度（5.5）和测试覆盖（1.0）。本计划分 4 个迭代周期，目标是在约 3-4 周内将项目从"可演示的骨架"推进为"可答辩的 MVP"。

| 迭代 | 主题 | 周期 | 目标评分变化 | 状态 |
|------|------|------|------------|------|
| Sprint 1 | 补齐核心闭环 | 第 1 周 | 功能完整度 5.5 → 7.5 | ✅ 已完成 |
| Sprint 2 | 安全加固与质量基线 | 第 2 周 | 安全性 6.0 → 8.0，测试 1.0 → 4.0 | ✅ 已完成 |
| Sprint 3 | 体验优化与性能 | 第 3 周 | UI/UX 8.5 → 9.0，后端 7.0 → 8.0 | 待开始 |
| Sprint 4 | 部署与收尾 | 第 4 周 | 部署 6.5 → 8.0，文档 8.0 → 9.0 | 待开始 |

---

## 二、Sprint 1 — 补齐核心闭环（第 1 周）

目标：打通"面试 → 错题 → 学习计划"断裂链路，让用户可以走完完整的业务闭环。

### Day 1-2：Interview 模块后端真实实现 ✅

**任务清单：**

- [x] 新建 `InterviewService` 接口和 `InterviewServiceImpl` 实现类
- [x] 实现 `start()`：根据方向从题库抽取 N 道题（默认 3-5 题），创建 `interview_session` 记录，返回第一道题
- [x] 实现 `current()`：根据 sessionId 查询当前题目（通过 `current_index` 定位）
- [x] 实现 `answer()`：
  - 调用 `AiOrchestratorService.scoreInterviewAnswer()` 获取评分
  - 解析 LLM 返回的结构化 JSON（分数、点评、标准答案、追问）
  - 写入 `interview_record` 表
  - 如果分数低于阈值（如 60 分），自动写入 `wrong_question` 表
  - 更新 `interview_session.current_index`
  - 如果已是最后一题，更新 session 状态为 `finished`，计算 `total_score`
- [x] 实现 `detail()`：查询 session 关联的所有 record，组装为 `InterviewDetailVO`
- [x] 修改 `InterviewController`，注入 `InterviewService`，替换硬编码实现
- [x] 优化 `DefaultAiOrchestratorService.scoreInterviewAnswer()`：
  - 构建结构化 Prompt，要求 LLM 返回 JSON 格式：`{"score": 85, "comment": "...", "standardAnswer": "...", "followUp": "..."}`
  - 使用 Jackson 解析 LLM 返回的 JSON，填充 `InterviewAnswerVO`
  - 移除固定分数 `78` 和硬编码占位文本

**关键接口变更：**

```
POST /api/interview/start    → 真实抽题，创建 session
GET  /api/interview/current/{sessionId} → 从 DB 查询当前题目
POST /api/interview/answer   → AI 评分 + 持久化 + 自动入错题
GET  /api/interview/detail/{sessionId} → 查询面试记录详情
```

### Day 3：Interview 模块前端对接 ✅

**任务清单：**

- [x] 新建 `src/api/interview.ts`，定义 `startInterviewApi`、`currentQuestionApi`、`submitAnswerApi`、`interviewDetailApi`
- [x] 重写 `InterviewPage.vue`：
  - 左侧面板：面试方向选择（下拉）、开始面试按钮、当前题目展示
  - 右侧面板：答案输入框（textarea）、提交按钮、评分结果展示（分数、点评、标准答案、追问）
  - 面试结束后展示整体结果和"查看错题"入口
  - Loading 状态和空状态处理
- [ ] 新建 `InterviewHistoryPage.vue` 或在 Interview 页面增加历史记录 Tab（延期至 Sprint 3）

### Day 4：Wrong 模块后端真实实现 ✅

**任务清单：**

- [x] 新建 `WrongService` 接口和 `WrongServiceImpl` 实现类
- [x] 实现 `list()`：根据 userId 分页查询 `wrong_question`，关联 `question` 表获取标题和分类
- [x] 实现 `detail()`：查询单条错题详情，关联标准答案和错误原因
- [x] 实现 `updateMastery()`：更新掌握状态（`not_started` → `reviewing` → `mastered`）
- [x] 实现 `delete()`：物理删除或逻辑删除错题记录
- [x] 修改 `WrongController`，注入 `WrongService`，替换硬编码实现
- [x] 在 `InterviewServiceImpl.answer()` 中确保低分题自动写入 `wrong_question`

**关键接口变更：**

```
GET    /api/wrong/list              → 分页查询当前用户错题
GET    /api/wrong/{id}              → 错题详情
PUT    /api/wrong/mastery/{id}      → 更新掌握状态
DELETE /api/wrong/delete/{id}       → 删除错题
```

### Day 4：Wrong 模块前端对接 ✅

**任务清单：**

- [x] 新建 `src/api/wrong.ts`，定义 `fetchWrongListApi`、`fetchWrongDetailApi`、`updateMasteryApi`、`deleteWrongApi`
- [x] 重写 `WrongPage.vue`：
  - 错题卡片列表，展示标题、来源、掌握状态标签
  - 点击卡片展开详情：标准答案、错误原因、用户作答
  - 掌握状态切换按钮（三态：未开始 / 复习中 / 已掌握）
  - "再次练习"入口（跳转到 Interview 或 Chat）
  - 删除功能
  - 空状态引导

### Day 5：Plan 模块后端真实实现 ✅

**任务清单：**

- [x] 新建 `PlanService` 接口和 `PlanServiceImpl` 实现类
- [x] 实现 `generate()`：
  - 查询当前用户的错题和薄弱点（按分类聚合）
  - 构建 Prompt，将薄弱点列表传给 LLM，要求生成 N 天学习计划
  - 解析 LLM 返回的结构化 JSON（每日任务列表）
  - 写入 `study_plan` 和 `study_plan_task` 表
- [x] 实现 `current()`：查询当前用户最新的 active 状态计划
- [x] 实现 `tasks()`：查询计划关联的所有任务
- [x] 实现 `updateTaskStatus()`：更新单个任务的状态
- [x] 修改 `PlanController`，注入 `PlanService`，替换硬编码实现

**关键接口变更：**

```
POST   /api/plan/generate              → AI 生成学习计划
GET    /api/plan/current               → 获取当前计划
GET    /api/plan/{id}/tasks            → 获取计划任务列表
PUT    /api/plan/task/{taskId}/status  → 更新任务状态
```

### Day 5：Plan 模块前端对接 ✅

**任务清单：**

- [x] 新建 `src/api/plan.ts`，定义 `generatePlanApi`、`fetchCurrentPlanApi`、`fetchPlanTasksApi`、`updateTaskStatusApi`
- [x] 重写 `PlanPage.vue`：
  - 左侧面板：计划生成入口（方向选择 + 生成按钮）、当前计划概览
  - 右侧面板：任务时间线，按日期分组展示
  - 任务状态切换（checkbox 或状态标签）
  - 完成率进度条
  - 空状态引导（"先完成一场面试，系统才能生成针对性计划"）

### Sprint 1 验收标准 ✅

- [x] 用户可以从 Dashboard 进入 Interview，完成一场 3-5 题的模拟面试
- [x] 面试结束后可以看到每题的 AI 评分、点评和标准答案
- [x] 低分题自动出现在 Wrong 页面
- [x] Wrong 页面可以查看错题详情、切换掌握状态
- [x] 可以基于错题生成学习计划，Plan 页面展示每日任务
- [ ] Dashboard 的数据指标（面试次数、平均分、错题数、计划完成率）随用户行为实时变化（待联调验证）
- [ ] 完整闭环可演示：登录 → Dashboard → 面试 → 错题 → 计划 → Dashboard（待联调验证）

---

## 三、Sprint 2 — 安全加固与质量基线（第 2 周）

目标：修复安全漏洞，建立测试基线，提升代码可维护性。

### Day 1：安全修复 ✅

**任务清单：**

- [x] **JWT 登出实现**：
  - 在 Redis 中维护 Token 黑名单（key: `jwt:blacklist:{token}`，TTL: Token 剩余有效期）
  - `AuthServiceImpl.logout()` 中将当前 Token 加入黑名单
  - `JwtAuthenticationFilter.doFilterInternal()` 中增加黑名单检查
  - 前端 `authStore.logout()` 逻辑不变（已有 `window.location.replace('/login')`）
- [x] **CORS 配置收紧**：
  - 开发环境：允许 `http://localhost:5173`
  - 生产环境：通过环境变量 `BYTECOACH_CORS_ORIGINS` 配置允许的域名列表
  - 修改 `CorsConfig` 读取配置
- [x] **JWT Secret 启动校验**：
  - 新建 `@PostConstruct` 或 `CommandLineRunner`，检查 `jwt-secret` 是否为默认值
  - 生产环境（非 dev profile）下如果是默认值则抛出异常阻止启动
- [x] **Chat 页面 XSS 防护**：
  - 安装 `dompurify`：`npm install dompurify && npm install -D @types/dompurify`
  - 修改 `ChatPage.vue` 的 `renderMarkdown()` 方法：`return DOMPurify.sanitize(marked.parse(value))`
  - 审查 `KnowledgePage.vue`：使用 `{{ }}` 插值，无 `v-html`，无需修改

### Day 2：`.gitignore` 修复与代码规范 ✅

**任务清单：**

- [x] **修复 `.gitignore`**：
  - 移除 `docs/` 排除规则（文档应纳入版本控制）
  - 移除 `deploy/` 排除规则（部署配置应纳入版本控制）
  - 添加 `backend/src/main/resources/application-prod.yml`（生产配置不入库）
  - 添加 `.env.production` 和 `.env.local`
- [x] **前端代码规范工具**：
  - 安装 ESLint + Prettier：ESLint 9 flat config + `eslint-plugin-vue` + `@typescript-eslint`
  - 创建 `eslint.config.js` 和 `.prettierrc` 配置文件
  - 在 `package.json` 中添加 `lint`、`lint:fix` 和 `format` 脚本
  - 需要 `npm install` 安装依赖后执行格式化
- [x] **后端代码规范检查**：
  - 在 `pom.xml` 中添加 `maven-checkstyle-plugin` 3.4.0 + Checkstyle 10.18.2
  - 创建 `checkstyle.xml` 基础规则文件（命名、导入、代码量、编码规范）
  - `failsOnError=false`，仅警告不阻断构建

### Day 3-4：核心模块测试 ✅

**后端测试任务清单：**

- [x] **Auth 模块测试**（`src/test/java/com/bytecoach/auth/`）：
  - `AuthControllerTest`：注册、登录、登出接口的 MockMvc 测试
  - 测试正常流程、用户名重复、密码错误、Token 过期等场景
- [x] **Chat 模块测试**（`src/test/java/com/bytecoach/chat/`）：
  - `ChatServiceImplTest`：会话创建、消息持久化、会话归属校验
  - Mock `AiOrchestratorService`，验证消息写入和会话更新逻辑
- [x] **Dashboard 模块测试**（`src/test/java/com/bytecoach/dashboard/`）：
  - `DashboardServiceImplTest`：Mock `DashboardMetricsMapper`，验证空数据和有数据两种场景
- [x] **Interview 模块测试**（Sprint 1 完成后）：
  - `InterviewServiceImplTest`：抽题逻辑、答题评分、自动入错题、session 状态流转
- [x] **Security 测试**：
  - `JwtTokenUtilTest`：Token 生成、解析、过期验证
  - `SecurityConfigTest`：白名单放行、ADMIN 权限控制

**前端测试任务清单：**

- [x] 安装 Vitest：`npm install -D vitest @vue/test-utils jsdom`
- [x] 在 `vite.config.ts` 中添加 test 配置
- [x] 编写 `stores/auth.test.ts`：login、logout、restoreProfile 的单元测试
- [x] 编写 `utils/http.test.ts`：请求拦截器、响应拦截器、401 处理的单元测试
- [ ] 编写关键组件的快照测试（可选）

### Day 5：接口分页 ✅

**任务清单：**

- [x] **后端分页改造**：
  - `QuestionServiceImpl.listQuestions()`：使用 `Page<Question>` + `LambdaQueryWrapper`
  - `KnowledgeServiceImpl.listDocs()`：同上
  - `WrongServiceImpl.list()`（Sprint 1 完成后）：同上
  - `ChatServiceImpl.listSessions()`：同上
  - 统一分页参数：`PageQuery` 基类增加 `pageNum`、`pageSize`（默认 1、20）
  - 返回值统一使用 `Result<IPage<T>>` 或自定义 `PageResult<T>`
- [x] **前端分页适配**：
  - 各列表页面增加 `el-pagination` 组件
  - API 调用增加分页参数传递

### Sprint 2 验收标准

- [x] JWT 登出后 Token 立即失效（可通过 Redis 查看黑名单 key 验证）
- [x] CORS 仅允许配置的域名
- [x] Chat 页面 Markdown 渲染无 XSS 风险（可通过 `<script>alert(1)</script>` 测试）
- [x] `.gitignore` 不再排除 `docs/` 和 `deploy/`
- [x] 后端核心模块测试通过率 100%，覆盖率 > 60%（Day 3-4）
- [x] 前端 `vitest` 测试通过（Day 3-4）
- [x] 列表接口支持分页，大数据量下不会一次性返回全部记录（Day 5）

---

## 四、Sprint 3 — 体验优化与性能（第 3 周）

目标：优化前端体验，修复性能瓶颈，提升代码质量。

### Day 1：前端体验优化

**任务清单：**

- [ ] **Vite 代理配置**：
  - 在 `vite.config.ts` 中添加 `server.proxy`，将 `/api` 代理到 `http://localhost:8080`
  - 修改 `.env.development`：`VITE_API_BASE_URL=/api`（相对路径）
  - 这样开发环境不再依赖 CORS
- [ ] **Interview 页面交互增强**：
  - 面试进度条（当前第 N 题 / 共 M 题）
  - 答题倒计时（可选，增加面试紧迫感）
  - 评分动画（数字从 0 滚动到目标分数）
  - 面试结束后的总结页面（总分、各题得分列表、"查看错题"按钮）
- [ ] **Chat 页面体验优化**：
  - 消息自动滚动到底部
  - 发送按钮增加 Enter 发送提示
  - 会话列表按最后消息时间排序（后端已支持，确认前端展示顺序）
- [ ] **Dashboard 图表增强**（可选）：
  - 引入 ECharts 或 Chart.js
  - 薄弱点用柱状图或雷达图展示
  - 学习趋势用折线图展示（需要新增历史数据接口）

### Day 2：后端性能优化

**任务清单：**

- [ ] **`ChatServiceImpl.send()` 事务范围缩小**：
  - 将 `@Transactional` 从 `send()` 方法移到内部的数据库操作方法
  - LLM 调用放在事务外，调用完成后再开事务写入结果
  - 避免长时间占用数据库连接
- [ ] **`KnowledgeRetrievalServiceImpl` 优化**：
  - 当前实现加载全部 chunk 到内存，改为数据库层面的 `LIKE` 查询
  - 在 `knowledge_chunk.content` 上添加 MySQL 全文索引：`ALTER TABLE knowledge_chunk ADD FULLTEXT INDEX ft_content (content)`
  - 使用 `MATCH ... AGAINST` 替代 Java 层的 `String.indexOf()`
  - 保留关键词评分逻辑，但在数据库层面做初步过滤
- [ ] **Dashboard 聚合查询缓存**：
  - 对 `DashboardMetricsMapper` 的查询结果添加 Redis 缓存
  - 缓存 key：`dashboard:overview:{userId}`，TTL：5 分钟
  - 用户完成面试或更新错题时主动清除缓存
- [ ] **LLM 调用超时与重试**：
  - 在 `OpenAiCompatibleLlmGateway` 中增加超时配置（当前 `timeout-seconds: 30` 已配置，确认 RestClient 使用了该值）
  - 增加简单的重试机制（最多 1 次重试，仅对 5xx 错误重试）

### Day 3：后端代码质量提升

**任务清单：**

- [ ] **DTO 参数校验补全**：
  - `ChatSendRequest`：`message` 增加 `@NotBlank`、`@Size(max = 2000)`；`mode` 增加 `@NotBlank`
  - `InterviewStartRequest`：`direction` 增加 `@NotBlank`；`questionCount` 增加 `@Min(1)`、`@Max(10)`
  - `InterviewAnswerRequest`：`questionId` 增加 `@NotNull`；`answer` 增加 `@NotBlank`
  - `GeneratePlanRequest`：`direction` 增加 `@NotBlank`；`days` 增加 `@Min(1)`、`@Max(30)`
  - `RegisterRequest`：`username` 增加 `@Size(min = 3, max = 64)`；`password` 增加 `@Size(min = 6, max = 128)`；`nickname` 增加 `@NotBlank`
  - 其他 DTO 按需补全
- [ ] **`wrong_question` 唯一约束**：
  - 执行 SQL：`ALTER TABLE wrong_question ADD UNIQUE INDEX uk_user_question (user_id, question_id)`
  - 修改 `InterviewServiceImpl` 中的入错题逻辑，使用 `INSERT ... ON DUPLICATE KEY UPDATE` 或先查后插
- [ ] **日志增强**：
  - 在 `AiOrchestratorService` 中增加 LLM 调用的入参和出参日志（脱敏后）
  - 在 `InterviewServiceImpl` 中增加面试评分结果日志
  - 在 `SecurityConfig` 中增加鉴权失败的详细日志
  - 修改 `application.yml` 中的 logback 配置，生产环境使用文件输出 + 日志轮转

### Day 4：前端代码质量提升

**任务清单：**

- [ ] **`ResultCode` 一致性确认**：
  - 检查后端 `ResultCode.SUCCESS` 的 code 值
  - 如果不是 `200`，修改前端 `http.ts` 的判断逻辑或后端的 code 值
  - 建议统一为 `200`
- [ ] **TypeScript 严格模式**：
  - 在 `tsconfig.app.json` 中开启 `"strict": true`
  - 修复由此产生的类型错误
  - 确保所有 API 调用都有正确的泛型类型
- [ ] **组件拆分优化**：
  - `AdminPage.vue` 当前约 460 行，考虑将三个 Tab 拆分为独立子组件：
    - `AdminCategoryTab.vue`
    - `AdminQuestionTab.vue`
    - `AdminKnowledgeTab.vue`
  - `DashboardPage.vue` 当前约 270 行，考虑拆分：
    - `DashboardGuideCard.vue`
    - `DashboardMetrics.vue`
    - `DashboardInterviews.vue`
    - `DashboardWeakPoints.vue`
- [ ] **错误边界处理**：
  - 在 `App.vue` 或 `AppLayout.vue` 中增加全局错误捕获
  - API 请求失败时展示友好的错误页面而非仅 ElMessage

### Day 5：联调与回归测试

**任务清单：**

- [ ] 端到端走通完整业务闭环：注册 → 登录 → Chat → Interview → Wrong → Plan → Dashboard
- [ ] 验证所有 Sprint 1-3 的改动
- [ ] 修复联调中发现的 Bug
- [ ] 更新 API 接口文档（Knife4j 自动生成，确认注解完整）

### Sprint 3 验收标准

- [ ] 开发环境通过 Vite 代理访问 API，无 CORS 问题
- [ ] `ChatServiceImpl.send()` 的 LLM 调用不在数据库事务内
- [ ] 知识检索使用数据库全文索引，不再全表加载到内存
- [ ] Dashboard 聚合查询有 Redis 缓存
- [ ] 所有 DTO 有完整的参数校验注解
- [ ] 前端 TypeScript strict 模式开启且无报错
- [ ] `AdminPage.vue` 拆分为子组件
- [ ] 完整闭环无阻塞性 Bug

---

## 五、Sprint 4 — 部署与收尾（第 4 周）

目标：完善部署方案，补充文档，准备答辩。

### Day 1：生产部署方案

**任务清单：**

- [ ] **后端 Dockerfile**：

```dockerfile
FROM eclipse-temurin:17-jre-alpine
COPY target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app.jar"]
```

- [ ] **前端构建与 Nginx 配置**：
  - 创建 `.env.production`：`VITE_API_BASE_URL=/api`
  - 创建 `deploy/nginx.conf`：前端静态文件 + API 反向代理到后端
  - 创建 `frontend/Dockerfile`：多阶段构建（Node 构建 + Nginx 部署）
- [ ] **docker-compose 生产版**：
  - 创建 `deploy/docker-compose.prod.yml`
  - 包含：MySQL、Redis、backend、frontend（Nginx）
  - 配置环境变量、健康检查、重启策略
  - 数据卷持久化
- [ ] **Spring Boot Actuator**：
  - 在 `pom.xml` 中添加 `spring-boot-starter-actuator`
  - 配置健康检查端点：`/actuator/health`
  - 配置 Docker Compose 的 healthcheck
- [ ] **生产环境配置**：
  - 创建 `application-prod.yml`：关闭 SQL 日志、配置日志文件轮转、收紧 CORS
  - 确保所有敏感配置通过环境变量注入

### Day 2：前端生产优化

**任务清单：**

- [ ] **构建优化**：
  - 在 `vite.config.ts` 中配置 `build.rollupOptions.output.manualChunks`，分离大依赖（element-plus、marked）
  - 配置 `build.target: 'es2020'`
  - 开启 gzip 压缩（Nginx 层面）
- [ ] **路由懒加载**：
  - 将所有页面组件改为动态 import：`() => import('@/pages/chat/ChatPage.vue')`
  - 减少首屏加载体积
- [ ] **`package.json` 版本锁定**：
  - 确认 `marked`、`element-plus` 等关键依赖的版本兼容性
  - 考虑使用 `package-lock.json` 锁定（已存在，确认是否提交到 Git）

### Day 3：文档更新

**任务清单：**

- [ ] **更新 README.md**：
  - 更新"当前实现状态"章节，标记 Interview/Wrong/Plan 为已完成
  - 添加生产部署说明（docker-compose.prod.yml）
  - 添加测试运行说明
- [ ] **更新接口文档**：
  - 确认 Knife4j 文档覆盖所有新增接口
  - 补充接口的 `@Operation`、`@Parameter` 等 OpenAPI 注解
- [ ] **更新数据库文档**：
  - 在 `04-数据库设计.md` 中补充 `wrong_question` 的唯一约束
  - 补充全文索引说明
- [ ] **创建 CHANGELOG.md**：
  - 记录本次开发计划的所有变更
- [ ] **更新 `.gitignore` 后确认所有需要的文件已提交**

### Day 4：答辩准备

**任务清单：**

- [ ] **演示脚本编写**：
  - 按照以下顺序准备演示流程：
    1. 登录进入 Dashboard，展示首次引导和空状态
    2. 进入 Chat，发起一次知识库问答，展示 RAG 引用
    3. 进入 Interview，开始一场模拟面试，展示 AI 评分和点评
    4. 进入 Wrong，展示自动沉淀的错题，切换掌握状态
    5. 进入 Plan，生成学习计划，展示每日任务
    6. 回到 Dashboard，展示数据变化（面试次数、错题数、计划完成率）
    7. 进入 Admin，展示题库和知识库管理
  - 准备每个步骤的预期效果截图
- [ ] **答辩要点准备**：
  - 架构设计取舍：为什么单体、为什么 MyBatis-Plus、为什么 Element Plus + Tailwind
  - AI 能力设计：LLM 网关抽象、Prompt 工程、结构化输出解析
  - RAG 设计：文档切分策略、关键词检索 vs 向量检索的权衡
  - 安全设计：JWT 无状态认证、Token 黑名单、CORS 策略
  - 二期规划：向量检索、用户上传、动态计划调整
- [ ] **准备常见面试问题的回答**：
  - "项目的并发量考虑？" → 单体架构 + Redis 缓存 + 数据库连接池
  - "AI 评分的准确性如何保证？" → 结构化 Prompt + 标准答案对比 + 分数阈值
  - "为什么不做微服务？" → MVP 阶段复杂度不值得，单体足够，保留拆分可能
  - "数据量大了怎么办？" → 分页 + 缓存 + 全文索引 + 后续向量数据库

### Day 5：最终验收

**任务清单：**

- [ ] 全量回归测试
- [ ] 确认 docker-compose 可一键启动完整环境
- [ ] 确认演示脚本可流畅执行
- [ ] 确认所有文档已更新
- [ ] Git 打 tag：`v1.0.0-mvp`

### Sprint 4 验收标准

- [ ] `docker-compose up -d` 可一键启动 MySQL、Redis、后端、前端
- [ ] 访问 `http://localhost` 可打开前端页面，API 正常工作
- [ ] 前端首屏加载时间 < 3 秒（本地环境）
- [ ] 所有文档与代码状态一致
- [ ] 演示脚本可完整执行无报错
- [ ] CHANGELOG.md 记录了所有变更

---

## 六、风险与应对

| 风险 | 概率 | 影响 | 应对方案 |
|------|------|------|---------|
| LLM 结构化输出解析失败 | 中 | 面试评分不可用 | 增加重试 + 兜底解析（正则提取分数）+ 默认评分 |
| Interview 抽题逻辑与题库数据不匹配 | 中 | 面试无法开始 | 先用简单随机抽题，确保题库有足够种子数据 |
| 前端改造工作量超预期 | 中 | Sprint 1 延期 | Interview/Wrong/Plan 优先保证功能可用，UI 细节放到 Sprint 3 |
| Redis 不可用导致 JWT 黑名单失败 | 低 | 登出功能降级 | 黑名单检查加 try-catch，Redis 不可用时跳过检查（降级为当前行为） |
| 全文索引对中文支持不佳 | 中 | 知识检索质量下降 | MySQL 8.x 支持 ngram 解析器，配置 `ngram_token_size=2` |

---

## 七、时间线总览

```
Week 1  ████████████████████████████████  Sprint 1: 核心闭环
        Interview后端 → Interview前端 → Wrong后端 → Wrong前端 → Plan后端+前端

Week 2  ████████████████████████████████  Sprint 2: 安全与质量
        安全修复 → 代码规范 → 后端测试 → 前端测试 → 分页改造

Week 3  ████████████████████████████████  Sprint 3: 体验与性能
        Vite代理 → 体验优化 → 性能优化 → 代码质量 → 联调回归

Week 4  ████████████████████████████████  Sprint 4: 部署与收尾
        Docker化 → 构建优化 → 文档更新 → 答辩准备 → 最终验收
```

---

## 八、完成后预期评分

| 维度 | 当前评分 | 预期评分 | 变化 |
|------|:-------:|:-------:|:----:|
| 整体架构设计 | 8.0 | 8.5 | +0.5 |
| 后端代码质量 | 7.0 | 8.0 | +1.0 |
| 前端代码质量 | 7.5 | 8.5 | +1.0 |
| 数据库设计 | 7.5 | 8.0 | +0.5 |
| 安全性 | 6.0 | 8.0 | +2.0 |
| UI/UX 设计 | 8.5 | 9.0 | +0.5 |
| 功能完整度 | 5.5 | 8.5 | +3.0 |
| 测试覆盖 | 1.0 | 5.0 | +4.0 |
| 部署与运维 | 6.5 | 8.0 | +1.5 |
| 文档与规范 | 8.0 | 9.0 | +1.0 |
| **综合评分** | **6.6** | **8.1** | **+1.5** |
