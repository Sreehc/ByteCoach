# MySQL 索引

MySQL 索引常见考点包括 B+Tree 结构、聚簇索引与二级索引区别、最左前缀匹配以及覆盖索引。

## 索引失效场景

MySQL 出现索引失效，通常和联合索引最左前缀被破坏、对索引列使用函数、发生隐式类型转换、范围查询后继续使用后续列有关。

# 事务与隔离级别

事务四大特性是 ACID。MySQL InnoDB 默认隔离级别是可重复读，通过 MVCC 和间隙锁处理并发一致性问题。

# 锁与执行计划

排他锁、共享锁、意向锁和间隙锁是常见锁模型。分析慢 SQL 时需要结合 explain、索引使用情况和回表成本。
